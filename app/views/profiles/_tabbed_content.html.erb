<%= render 'group_pages/statement_tabs',
    group:         @info.group,
    state:         @info.state,
    accepted_path: group_profile_path(@info.group.name),
    voting_path:   profile_path(@info.group.name, :voting),
    alive_path:    profile_path(@info.group.name, :alive),
    rejected_path: profile_path(@info.group.name, :rejected),
    dead_path:     profile_path(@info.group.name, :dead) %>

<%= render 'group_pages/proposals_list', group: @info.group, info: @info, state: @info.state %>
