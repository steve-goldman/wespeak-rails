<% provide(:title, @info.group.name) %>
<div class="row">
  <div class="col-md-offset-3 col-md-6 col-xs-12">
    <p><%= link_to "Back to #{@info.group.name}", group_profile_path(@info.group.name) %></p>
    <div class="why-not-eligible">
      <% if @info.active_member.nil? %>
        You are not active in the group.  Only active members may vote on statements.
      <% else %>
        The vote began on <%= Constants::Timespans::full_time(@statement.vote_began_at) %>
        but you last became active in the group on <%= Constants::Timespans::full_time(@info.active_member.created_at) %>.
        Only members active when a vote begins may vote.
      <% end %>
    </div>
  </div>
</div>
