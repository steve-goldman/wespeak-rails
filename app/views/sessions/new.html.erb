<% provide(:title, "Log In") %>

<% if !logged_in? %>
  <%= render 'login' %>
<% else %>
  <p>Please <%= link_to "Log out", logout_path, method: "delete" %> if you want to log in as a different user</p>
<% end %>

