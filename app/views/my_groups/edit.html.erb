<% provide(:title, @group.name + " Configuration") %>
<div class="row">
  <div class="col-md-offset-3 col-md-6 col-xs-12">
    <%= render 'edit_participation_filters', group: @group %>
    <%= render 'edit_rules',                 group: @group %>
  </div>
</div>
