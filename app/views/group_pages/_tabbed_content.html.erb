<%= render 'group_pages/statement_tabs', active_tab: active_tab %>
<% if @info.state == :alive && @info.change_eligible? %>
  <%= render "#{active_tab}/new" %>
<% end %>
<%= render "#{active_tab}/index" %>
