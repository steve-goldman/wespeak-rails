<%= form_for :unsupport, url: unsupport_path(@info.group.name), method: :delete, remote: true do |f| %>
  <%= hidden_field_tag :statement_id, statement.id %>
  <%= hidden_field_tag :state, @info.state %>
  <%= f.submit "Unsupport", class: "btn" %>
<% end %>
