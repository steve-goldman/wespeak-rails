<%= form_for :support, url: support_path(@info.group.name), remote: true do |f| %>
  <%= hidden_field_tag :statement_id, statement.id %>
  <%= f.submit "Support!", class: "btn btn-primary" %>
<% end %>
