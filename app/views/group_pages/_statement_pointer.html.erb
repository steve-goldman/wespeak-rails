<%= render('initial_group/show',              statement: statement.get_content) if statement.type == :initial_statement         %>
<%= render('display_names/show',              statement: statement.get_content) if statement.type == :display_name              %>
<%= render('taglines/show',                   statement: statement.get_content) if statement.type == :tagline                   %>
<%= render('updates/show',                    statement: statement.get_content) if statement.type == :update                    %>
<%= render('rules/show',                      statement: statement.get_content) if statement.type == :rule                      %>
<%= render('invitations/show',                statement: statement.get_content) if statement.type == :invitation                %>
<%= render('group_email_domain_changes/show', statement: statement.get_content) if statement.type == :group_email_domain_change %>
<%= render('profile_images/show',             statement: statement.get_content) if statement.type == :profile_image             %>
<%= render('locations/show',                  statement: statement.get_content) if statement.type == :location                  %>
