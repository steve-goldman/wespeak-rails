$('#proposals-list').append('<%= j render 'statements' %>');
<% if @statements.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @statements %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
