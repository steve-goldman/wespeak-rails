<%= render 'group_pages/statement_tabs', active_tab: :invitations %>
<% if @info.state == :alive && @info.change_eligible? %>
  <%= render 'invitations/new' %>
<% end %>
<%= render 'invitations/index' %>
