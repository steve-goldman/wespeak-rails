<%= render 'groups/locations_show', latitude: @group.latitude, longitude: @group.longitude, radius: @group.radius, change_link: false %>

<%= form_for @group, url: update_locations_group_path(@group.id), method: :patch do |f| %>

<%= render 'groups/locations_fields', f: f, radius: @group.radius, button_text: "Save" %>

<% end %>
