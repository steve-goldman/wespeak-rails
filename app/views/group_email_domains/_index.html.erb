<div class="group-email-domains">
  <h4>Email Address Restrictions Settings</h4>
  <p>Restrict users based on email addresses?  Leaving this empty makes no restrictions.  Otherwise, users will not be restricted if they have an email address that matches one of the domains.  Remember, anything you set here can be changed later by the group.</p>
  <% if @group.group_email_domains.any? %>
  <table class="table table-hover table-striped">
    <thead>
      <tr>
        <th>Domain</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @group.group_email_domains.each do |group_email_domain| %>
      <tr>
        <td><%= group_email_domain.domain %></td>
        <td>
          <% warning = "Are you sure you want to delete " + group_email_domain.domain + "?" %>
          <%= link_to my_group_group_email_domain_path(my_group_id: @group.id, id: group_email_domain.id), method: :delete, data: { confirm: warning } do %>
          <i class="glyphicon glyphicon-trash"></i>
          <% end %>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>
  
  <%= form_for(:group_email_domain, url: my_group_group_email_domains_path(@group.id)) do |f| %>
  <%= f.label :email, "Add an Email Domain" %>
  <%= f.text_field :domain, placeholder: "Email Domain", class: "form-control" %>
  <%= f.submit "Add Domain", class: "btn btn-primary" %>
  <% end %>
</div>
