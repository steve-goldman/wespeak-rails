$('#comments-list').append('<%= j render 'show_comments' %>');
<% if @comments.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @comments %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
