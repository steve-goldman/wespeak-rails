<ul class="nav nav-pills nav-stacked">
  <%= content_tag(:li, link_to("General",          "#"),                            class: (yield(:active_tab) == "general"          ? "active" : "inactive")) %>
  <%= content_tag(:li, link_to("Email Identities", settings_email_identities_path), class: (yield(:active_tab) == "email_identities" ? "active" : "inactive")) %>
</ul>
