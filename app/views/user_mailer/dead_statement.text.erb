Dear WeSpeak user,

A statement that you created of type <%= StatementTypes.name(@statement.statement_type) %> in the group <%= @statement.group.name %> has died because it did not have enough support.

Click the following link to view it.

<%= proposal_url(@statement.group.name, @statement.id) %>

You received this email because you are a WeSpeak member interested in this group.

Click the following link to manage your email preferences.

<%= edit_settings_notifications_url %>

