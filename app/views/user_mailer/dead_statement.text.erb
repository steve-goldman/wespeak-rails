Dear WeSpeak user,

A statement that you created has died because it did not have enough support in <%= render 'group_name', group: @statement.group, period: true %>

<%= render 'statement', statement: @statement %>

You received this email because you are a WeSpeak member interested in your statements.

<%= render 'mailer_footer' %>
