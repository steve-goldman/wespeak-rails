Dear WeSpeak user,

A statement that you created in @<%= @statement.group.name %> has died because it did not have enough support.

Click the following link to view it.

<%= statement_url(@statement.group.name, @statement.id) %>

You received this email because you are a WeSpeak member interested in your statements.

<%= render 'mailer_footer' %>
