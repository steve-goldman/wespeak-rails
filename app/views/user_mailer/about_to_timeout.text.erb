Dear WeSpeak user,

Your membership will expire on <%= Constants::Timespans::full_time(@expires_at) %> in <%= render 'group_name', group: @group, period: true %>
You won't be eligible to support or vote on new statements if it expires.

Click the following link to visit the group page, where you can renew your membership.

<%= group_profile_url(@group.name) %>.

You received this email because you are a WeSpeak member interested in knowing when your memberships are nearing expiration.

<%= render 'mailer_footer' %>
