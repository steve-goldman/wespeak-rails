Dear future WeSpeak user,

You have been invited to join @<%= @group.name %>.

Click the following link to sign up for WeSpeak.  The invitation will be there waiting.

<%= new_user_url %>.

<%= render 'mailer_footer' %>
