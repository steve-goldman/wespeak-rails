Dear WeSpeak user,

Voting has ended for a statement in <%= render 'group_name', group: @statement.group, period: true %>

<%= render 'statement', statement: @statement %>

You received this email because you are a WeSpeak member interested in this group.

<%= render 'mailer_footer' %>
