Dear WeSpeak user,

You have been invted to join <%= render 'group_name', group: @group, period: true %>
<%= render 'group_summary', group: @group %>

Click the following link to visit the group page.

<%= group_profile_url(@group.name) %>.

You received this email because you are a WeSpeak member interested in knowing when you receive invitations.

<%= render 'mailer_footer' %>
